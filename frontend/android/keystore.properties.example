# Copie ce fichier en "keystore.properties" (meme dossier) et remplis tes valeurs.
# keystore.properties est gitignore : il ne sera JAMAIS commite.
#
# storeFile = chemin du .jks RELATIF a frontend/android/ (mets le .jks ici)
storeFile=onskone-upload.jks
storePassword=TON_MOT_DE_PASSE_KEYSTORE
keyAlias=onskone-upload
keyPassword=TON_MOT_DE_PASSE_CLE
